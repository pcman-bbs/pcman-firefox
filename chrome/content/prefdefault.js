// Properties below will bind to the element in the prefWindow with the same id
// Don't use '_url' as the name of the new preference

'use strict';

var EXPORTED_SYMBOLS = ["PrefDefaults"];

var PrefDefaults = {
    '_url': 'default',
    'Encoding': 'system'
};

